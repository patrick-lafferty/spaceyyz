/*var assert = require('chai').assert;
window.firebase = require('./mocks/firebase');

describe('Testing Services', function () {
	beforeEach(angular.mock.module('spaceyyz'));

	var engineFactory;

	beforeEach(inject(function(_engineFactory_) {
		engineFactory = _engineFactory_;
	}));

	it('should exist', function () {
		assert.isDefined(engineFactory);
	});

	describe('.getEngines()', function () {
		it('should exist', function () {
			assert.isDefined(engineFactory.getEngines);
			assert.isFunction(engineFactory.getEngines);
		});

		it('should run', function () {
			engineFactory.getEngines();	
		});
	});
});*/
